{
  "devDependencies": {
    "elm": "^0.19.0-bugfix2",
    "elm-format": "^0.8.0"
  },
  "scripts": {
    "test": "elm make --docs=documentation.json && (cd examples && elm make Main.elm --output ../docs/index.html) && elm-format --validate ."
  }
}
