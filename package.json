{
  "devDependencies": {
    "elm": "^0.19.0-bugfix6",
    "elm-format": "^0.8.1"
  },
  "scripts": {
    "test": "elm make --docs=documentation.json && mkdir -p docs && (cd examples && elm make Main.elm --output ../docs/index.html) && elm-format --validate ."
  }
}
